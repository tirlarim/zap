#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "./ballsortpuzzle.h"
#include "c4.h"

#define sizeX 6
#define sizeY 4


int main() {
//  ball_sort_puzzle();

//  char** field = (char**)malloc(sizeY*sizeof(*field));
//  for (int i = 0; i < sizeY; ++i) {
//    field[i] = (char*)calloc(sizeX, sizeof(*field[i]));
//  }
//  generator(sizeY, sizeX, field);
//  game_field(sizeY, sizeX, field);
//  down_possible(sizeY, sizeX, field, 6, 3);
//  game_field(sizeY, sizeX, field);
//  printf(check(sizeY, sizeX, field) ? "Arena valid\n" : "Arena invalid\n");

//  int rows = 4;
//  int cols = 5;
//  char board[rows][cols]; // -Wno-vla-parameter-Wno-vla-parameter-Wno-vla-parameter-Wno-vla-parameter-Wno-vla-parameter ok :^)
//  initialize_board(rows, cols, board);
//  print_board(rows, cols, board);
//  drop_piece(rows, cols, board, 4, 'x'); drop_piece(rows, cols, board, 4, 'x');
//  drop_piece(rows, cols, board, 4, 'x'); drop_piece(rows, cols, board, 4, 'o');
//  drop_piece(rows, cols, board, 3, 'x'); drop_piece(rows, cols, board, 3, 'x');
//  drop_piece(rows, cols, board, 3, 'o');
//  drop_piece(rows, cols, board, 2, 'x');  drop_piece(rows, cols, board, 2, 'o');
//  drop_piece(rows, cols, board, 1, 'o');
//  print_board(rows, cols, board);
//  if (check_win(rows, cols, board, -1, 1, 'o')) {
//    printf("Win");
//  }

//  play();
  return 0;
}
